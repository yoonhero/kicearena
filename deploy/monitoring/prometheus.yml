global:
  scrape_interval: 15s

scrape_configs:
  - job_name: kice-arena
    metrics_path: /metrics
    authorization:
      type: Bearer
      credentials_file: /run/secrets/kice_arena_metrics_token
    static_configs:
      - targets:
          - kice-arena:3001
